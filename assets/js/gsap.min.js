/*!
 * GSAP 3.12.2 Animation Engine placeholder / fallback loader
 */
window.gsap = window.gsap || {
  to: function(target, config) { console.log('GSAP to', target, config); },
  from: function(target, config) { console.log('GSAP from', target, config); },
  fromTo: function(target, fromConfig, toConfig) { console.log('GSAP fromTo', target, fromConfig, toConfig); }
};
